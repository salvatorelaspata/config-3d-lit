import * as THREE from 'three';
export declare const applyTextureOnMesh: (obj: THREE.Object3D, customTexture: boolean, hdrEquirect: THREE.Texture, texture: THREE.Texture) => void;
export declare const use3DViewer: (mount: HTMLDivElement | undefined, modelConfig: Viewer3dType) => void;
